[Resolve]
# Use DHCP-provided DNS by default
# Run setup-dns to switch to Cloudflare or custom DNS

# Disable multicast DNS (delegate to Avahi for printer discovery)
MulticastDNS=no

# Enable DNSSEC validation
DNSSEC=allow-downgrade

# Cache settings
Cache=yes
CacheFromLocalhost=no
