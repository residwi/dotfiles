# UWSM environment - changes require restart to take effect

export TERMINAL=ghostty
export EDITOR=nvim
export VISUAL="$EDITOR"

# Activate mise if installed
command -v mise &>/dev/null && eval "$(mise activate bash)"
