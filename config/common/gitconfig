[user]
  name = Resi Dwi
  email = 37761892+residwi@users.noreply.github.com

[include]
  path = ~/.gitconfig.personal

[core]
  excludesfile = ~/.gitignore

[color]
  ui = true

[init]
  defaultBranch = main

[commit]
  gpgsign = true
	verbose = true           # Include diff comment in commit message template

[push]
  autoSetupRemote = true   # Set upstream on first push
  followTags = true        # Push annotated tags with commits

[pull]
	rebase = true 			     # Rebase (instead of merge) on pull

[diff]
  algorithm = histogram    # Clearer diffs on moved/edited lines
  colorMoved = plain       # Highlight moved blocks in diffs
	mnemonicPrefix = true    # More intuitive refs in diff output

[rerere]
  enabled = true           # Record and reuse conflict resolutions
  autoupdate = true        # Apply stored conflict resolutions automatically

[merge]
  conflictStyle = zdiff3   # Better conflict markers

[fetch]
  prune = true             # Prune deleted branches on fetch
  pruneTags = true         # Prune tags that no longer exist on the remote

[rebase]
  autoStash = true         # Automatically stash local changes before rebasing
  autoSquash = true        # Automatically fixup/squash commits during rebase

[column]
	ui = auto 			         # Output in columns when possible

[branch]
  sort = -committerdate    # Sort branches by most recent commit first

[tag]
	sort = -version:refname  # Sort version numbers as you would expect

[includeIf "gitdir:~/Work/"]
  path = ~/Work/.gitconfig
